# 現在地（毎回ここを最初に読む）

最終更新: 2026-09-04 / 更新者: Claude (Opus 5)

このファイルは**常に上書き**する。長い経緯は書かない。
「いま何が終わっていて、次に何をするか」だけを置く場所。

---

## 1. いまの状態

- ブランチ: `agent/external-booking-conflict`（`main` ではない）
- **本番には何も出していない。** main へのマージ = 自動デプロイなので、マージ自体が保留中。
- worker 772 tests / db 145 tests / 管理画面ビルド すべて通過。`tsc --noEmit` clean。

### このブランチに積んであるもの（新しい順）

| commit | 内容 | 本番影響 |
|---|---|---|
| `788bfac` | 権限ガード（一斉配信・スコア・計測リンク・流入経路・プールの書き込みを owner/admin に）、PIIログ削除、キャンペーンリマインダを40件/tickに制限 | 未反映 |
| `a8beed3` | タグ管理画面 `/tags` 新設、タグ改名API、削除時の参照チェック、友だち情報欄の編集UI | 未反映 |
| `1657bcd` | 現状調査の記録（`MIGRATION-proline-to-os.md` 5章） | ドキュメント |
| `d18e780` | 移行方針の記録（`MIGRATION-proline-to-os.md`） | ドキュメント |
| `22e940c` | リッチメニューの原因記録（`HANDOVER-2026-09-02-claude.md` 10章） | ドキュメント |
| `8096625` | `POST /api/line-accounts/:id/rich-menu/unlink-all`（個別割り当ての一括解除） | 未反映 |
| `f71c13f` | Codex 向け引き継ぎ | ドキュメント |

読む順番: `RESUME.md`（これ） → `MIGRATION-proline-to-os.md` → `HANDOVER-2026-09-02-claude.md`

---

## 2. 決まっていること

- **鈴木薬舗OS を主にする。プロラインはチケットが切れたら終了。** 段階的に移す。
- 原則: 1つの機能の持ち主は必ずどちらか一方。可逆であること。リッチメニューは削除しない。
- 着手順は KOTA が選択済み: ①タグ・友だち情報欄 → ②危険の修正 → ③シナリオ分岐UI/オートメーション → ④一斉配信の強化

---

## 3. 次の一手

### ③ シナリオの分岐UI ＋ オートメーションの選択式化（未着手）

- シナリオのステップ編集画面に `conditionType` / `conditionValue` / `nextStepOnFalse` の入力を出す。
  エンジン側（`services/step-delivery.ts:364-471`）は既に動く。**画面に項目が無いだけ。**
- オートメーション画面（`apps/web/src/app/automations/page.tsx:281-321`）の
  生JSONテキストエリアを選択式に。`switch_rich_menu` / `remove_rich_menu` もここにしか無い。
- **着手前に確認が要る（下記 4-B）。**

---

## 4. KOTA への保留中の質問（次回まとめて聞く）

**A. `staff_members` の役割**
1件だけ登録があり `role='staff'`。②で友だち情報欄の書き換えと一斉配信を owner/admin 限定にしたため、
**このままだとその方は編集できない。** owner か admin に上げるか、編集不可のままにするか。
（オーナーとしてのログインは環境変数 `API_KEY` 経由なので、KOTA 本人の操作には影響しない）

**B. 本番のオートメーション設定の件数**
③でJSON手書き欄を選択式に作り替える。既存データは変換せずそのまま読める作りにする予定だが、
**本番に手で組んだ設定が何件あるか私からは見えない。** 0件なら気にせず進められる。

---

## 5. 別系統で止まっているもの（ブラウザ接続待ち）

Phase 1（リッチメニュー）が未完。**Chrome拡張が未接続で、プロラインを開けない。**

- 残: プロラインの超ステップ配信「このシナリオ登録時に適用されるリッチメニュー」を全部「なし」にする
- そのあと: main へマージ → `unlink-all` を dryRun → `dryRun:false` で実行 → 実機確認
- **順番厳守。** プロライン側を止める前に unlink しても、再び貼り直されるだけ。

現在の本番設定（2026-09-02 時点、変更なし）:
- LINE デフォルト = `richmenu-ee0330cf4a660bc90a2d54383a9d434a`
- プロライン「基本リッチメニュー」= `LINE`（なし）
- 旧メニュー22件は削除していない
- 戻し方: プロラインのドロップダウンを `8:基本リッチメニュー(6分割)`（`OuZDs64N8P`）に戻し、
  「はい、全ユーザーのリッチメニューを変更してください」を選ぶ

---

## 6. 本番を見ないと分からないこと（推測で埋めない）

- 適用済みマイグレーション（`SELECT name FROM _migrations`）。**063 が未適用の可能性**
- プロラインに生きているシナリオ・ステップ配信の本数
- 本番リッチメニューが LIFF のどちらの予約画面を指しているか（`?page=book` と `?page=salon-book` の2系統）

---

## 7. 作業環境の癖（Claude 側の制約）

- ファイルブリッジは**削除ができない**（`rm` 不可、`mv` は可）。git のロックが残りやすいので、
  `git add` と `git commit` の**間**で `.git/index.lock` と `.git/HEAD.lock` を `_to_delete/` へ退避する。
- コミット時は identity を明示: `git -c user.name='鈴木賢太郎' -c user.email='kentaro@MacBook-Pro.local'`
- Mac の `node_modules` は Claude 側の Linux VM で動かない。テストはクラウド側の複製で実行している。
- `_to_delete/` に退避したゴミが溜まっている。中身を確認のうえ削除してよい。
